import DefaultLayout from '@/layouts/DefaultLayout';
import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <DefaultLayout>
      <div></div>
    </DefaultLayout>
  );
};

export default Home;
